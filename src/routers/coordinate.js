export const goToHomePage = (history) => {
  history.push('/');
};

export const goToLoginPage = (history) => {
  history.push('/login');
};

export const goToCreateMusicPage = (history) => {
  history.push('/create-music');
};

export const goToListMusicPage = (history) => {
  history.push('/list-music');
};
